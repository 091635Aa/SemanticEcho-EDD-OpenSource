# 复现方案（P6 EDD）

## 一、本仓库包含什么

| 类别 | 内容 |
|---|---|
| 核心源码 | `情感导演解码/`：情感导演解码器 + 目标决策器 + 锚点库 + 锚点解码器 + 接口降级（5 文件闭合依赖） |
| 评测脚本 | `基准脚本/`：统一生成器（8 模式：裸/P1/P1.5/P2.5/P3/P4/P5/P6）、裁判子进程（7B 4bit）、5 大基准 run 脚本、7 模式汇总、多模型泛化脚本 |
| 实验数据 | `实验数据/`：5 大基准结果 JSON、泛化测试证据、全流程报告、EDD 实验报告 |

## 二、环境

- Python 3.10+，PyTorch（CUDA 版），transformers，numpy，jieba
- 评测用模型：
  - 生成器：`Qwen2.5-1.5B-Instruct`（主模型，fp16）
  - 裁判：`Qwen2.5-7B-Instruct`（4bit，bnb 量化）
  - 泛化：`Qwen2.5-3B-Instruct`、`Qwen3-1.7B-Instruct`
- 评测协议：7 模式 × 5 大图灵基准 × 种子 2026，全流程同样本同裁判，结果可直接横向对比

> 注意：完整评测（统一生成器 8 模式）依赖完整工程（P1~P5 各解码器 + semantic_echo 包）。
> 本仓库发布的是 **P6 EDD 核心模块** + 评测脚本 + 实测数据；如需端到端重跑全部 8 模式，
> 请按 基准脚本 顶部注释补全工程依赖。EDD 核心模块本身仅依赖本仓库 5 个文件。

## 三、最小复现（EDD 核心）

```python
import sys
sys.path.insert(0, "情感导演解码")
from transformers import AutoModelForCausalLM, AutoTokenizer
from 锚点库 import 锚点库
from 目标决策器 import 目标决策器
from 情感导演解码器 import 情感导演解码器

model = AutoModelForCausalLM.from_pretrained("Qwen/Qwen2.5-1.5B-Instruct", torch_dtype="auto")
tok = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-1.5B-Instruct")
锚 = 锚点库(model, tok, 维度名=["温柔", "开心", "难过", "愤怒", "害怕", "平静"])
决策 = 目标决策器(锚点库=锚)
dec = 情感导演解码器(model, tok, 锚, 决策,
                   开启DMR=True, 开启KV调制=True, 开启V调制=True,
                   开启锚点偏置=True, 开启DSA=True,
                   α基=0.18, κ基=0.20, κ_v基=0.12, β基=0.6,
                   AI腔抑制强度=2.0, 口语化强度=0.6,
                   任务自适应=True, 进度调度=True, 在线纠正=True, 句子停止=True)
# 生成：dec.生成(input_ids, max_new_tokens=256, 用户文本=..., 指令=..., 角色=...)
```

## 四、完整评测复现（5 大基准 × 7 模式）

```
基准脚本/统一生成器.py        # 8 模式统一生成入口（P6 人格注入 + EDD 实例化参数）
基准脚本/统一_run_heartbench.py   # HeartBench（中文人味，rubric 命中）
基准脚本/统一_run_feel_heart.py   # HEART-BENCH（记忆驱动人格推理 + 共情）
基准脚本/统一_run_llm_judge.py    # LLM-as-Judge（AI vs 真人盲评）
基准脚本/统一_run_turingbench.py  # TuringBench（中文体系图灵检测）
基准脚本/统一_run_emocharacter.py # EmoCharacter v2（角色扮演保真度，差分校正）
基准脚本/run_all_7模式.py         # 汇总 → 全流程报告
```

评测采用两阶段架构（生成进程退出后独立裁判进程），规避 1.5B 生成器与 7B 裁判同进程 OOM；
裁判子进程带 4bit 加载重试（bnb 0.50 与 transformers 偶发段错误时自动重试）。

## 五、关键结果（种子 2026，Qwen2.5-1.5B）

| 模式 | HeartBench | HEART-BENCH | LLM-Judge | TuringBench | EmoCharacter | 综合 |
|---|---|---|---|---|---|---|
| 裸 | 0.4783 | 0.5692 | 0.4100 | 0.5333 | 0.9090 | 0.5800 |
| P5 超融合 | 0.4909 | 0.3203 | 0.5067 | 0.9333 | 0.8762 | 0.6255 |
| **P6 情感导演** | **0.4898** | **0.5217** | **0.7067** | **0.9333** | **0.8713** | **0.7046** |

综合 0.7046 为 7 模式第一。维度级提升：LLM-Judge 胜率 +122%、HEART-BENCH +63%、
多模型泛化 +67%~+233%。详见 `实验数据/全流程_报告_2026.md` 与 `实验数据/P6_情感导演_实验报告_2026.md`。
